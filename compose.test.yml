services:
  web:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER:-loresuelvo}/loresuelvo-admin-webapp:${IMAGE_TAG:-main}
    container_name: loresuelvo-admin-test
    ports:
      - "3000:3000"
    env_file:
      - .env.test
    environment:
      NODE_ENV: production
    restart: unless-stopped

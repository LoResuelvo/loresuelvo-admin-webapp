services:
  web:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER:-matexore}/loresuelvo-admin-webapp:main
    container_name: loresuelvo-admin-test
    ports:
      - "3000:3000"
    env_file:
      - .env.test
    environment:
      NODE_ENV: production
    restart: unless-stopped
